# verify_race_condition.sh 実行結果(フェーズ0対応の前後比較)

対象: `stock_in.php`(および `stock_out.php`)の在庫更新ロジック
コマンド: `docker compose exec legacy-web bash /var/www/verify_race_condition.sh 1 10 10`
(商品ID=1 に対し「+10 の入庫」を 10 本同時実行 → 期待値は開始前 +100)

---

## 修正前(レガシー: SELECT → PHPで加算 → UPDATE)

3 回連続で lost update が再現。

| 実行 | 開始前 | 終了後 | 期待値 | 結果 |
|------|-------|-------|-------|------|
| 1 | 125 | 165 | 225 | ★ lost update(60個消失) |
| 2 | 125 | 195 | 225 | ★ lost update(30個消失) |
| 3 | 125 | 175 | 225 | ★ lost update(50個消失) |

```
=== 結果確認 ===
開始前の在庫: 125
終了後の在庫: 165
期待値(125 + 10 x 10): 225

★ lost update が発生しました(60個分が消失)
```

## 修正後(アトミック更新: UPDATE ... SET stock_quantity = stock_quantity + ? WHERE id = ?)

5 回連続で消失ゼロ。加えて 20 本同時(期待 +200)でも一致。

| 実行 | 開始前 | 終了後 | 期待値 | 結果 |
|------|-------|-------|-------|------|
| 1 | 125 | 225 | 225 | OK(消失なし) |
| 2 | 125 | 225 | 225 | OK(消失なし) |
| 3 | 125 | 225 | 225 | OK(消失なし) |
| 4 | 125 | 225 | 225 | OK(消失なし) |
| 5 | 125 | 225 | 225 | OK(消失なし) |
| 20本同時 | 125 | 325 | 325 | OK(消失なし) |

```
=== 結果確認 ===
開始前の在庫: 125
終了後の在庫: 225
期待値(125 + 10 x 10): 225

今回はタイミングにより競合が発生しませんでした。もう一度実行してみてください。
```

※ スクリプトの「今回はタイミングにより競合が発生しませんでした」は
   `終了後 == 期待値` のときのメッセージ。修正後は毎回このパスに入る。

## 出庫側(TOCTOU)の追加確認

商品ID=4 の在庫を 5 にし、「qty=5 の出庫」を 5 本同時実行:

- 修正前ロジック相当: 複数リクエストが「在庫あり」と誤判定し在庫がマイナスになり得る
- 修正後: `WHERE id = ? AND stock_quantity >= ?` の条件付き UPDATE により
  **最終在庫 = 0(マイナスにならない)**。成功は 1 本のみ、残りは在庫不足扱い。

---

## characterization_tests.sh(挙動不変の確認)

修正前・修正後とも **10 passed / 0 failed**(HTTP 入出力の挙動は維持)。
