services:
  legacy-mysql:
    image: 'mysql:5.6'
    environment:
      - MYSQL_ROOT_PASSWORD=${DB_PASSWORD}
    ports:
      - "3307:3306"
    volumes:
      - ./schema.sql:/docker-entrypoint-initdb.d/schema.sql

  legacy-web:
    build: './php56'
    environment:
      - DB_HOST=${DB_HOST}
      - DB_USER=${DB_USER}
      - DB_PASSWORD=${DB_PASSWORD}
      - DB_NAME=${DB_NAME}
    ports:
      - "8082:80"
    volumes:
      - "./www:/var/www/html"
      - "./verify_race_condition.sh:/var/www/verify_race_condition.sh"
      - "./characterization_tests.sh:/var/www/characterization_tests.sh"
      - "./schema.sql:/var/www/schema.sql"
    depends_on:
      - legacy-mysql

  legacy-adminer:
    image: adminer:4.6
    depends_on:
      - legacy-mysql
    ports:
      - "8083:8080"
